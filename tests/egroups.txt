Tests for the e-groups querier. Non-recursive queries

    >>> from egroup_filter_generator import egroup_filter_generator
    >>> rs1 = egroup_filter_generator('cert-d')
    >>> type(rs1)
    <type 'list'>
    >>> filter(lambda x: type(x) != type(""), rs1)
    []
    >>> len(l) > 1
    True

Since cert-sec is a member of cert-d, l is won't list its members (it
wasn't recursive)

    >>> rs2 = egroup_filter_generator('cert-sec')
    >>> type(rs2)
    <type 'list'>
    >>> s1 = set(rs2)
    >>> s2 = set(rs1)
    >>> s1 - s2 == s1
    True

Recursive queries.

    >>> l2 = egroup_filter_generator('cert-d', recursive=True)
    >>> type(l2)
    <type 'list'>
    >>> len(l2) > len(l)
    True
    >>> s2=set(l2)
    >>> s2 >= s1
    True
